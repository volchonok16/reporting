-- Колонка «Маржа» убрана из вкладки «Влияние по выручке» (gid=revenue).
-- Не перезаписывает cells: ключ Маржа в JSONB может остаться, UI его не показывает.

COMMENT ON COLUMN revenue_activity_row.cells IS
  'JSON: Активность, Статус F2 2026, Ответственный, влияния (база/выручка/gmc), Комментарий';

SELECT 1;
