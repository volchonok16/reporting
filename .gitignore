# PostgreSQL / Docker
*.log
.env
.env.local

# Локальные ключи (Google SA и др.) — только на диске, не в git
secrets/*.json

# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# PlantUML export (генерируйте локально при необходимости)
plantuml/out/

# Offline Docker bundle (собирается локально, переносится scp)
dist/*.tar
dist/*.manifest.txt

# Voice (carousel) local artifacts
voice/node_modules/
voice/.next/
voice/build/
voice/data/*.sqlite3-shm
voice/data/*.sqlite3-wal
voice/data/uploads/
voice/data/workspaces/
